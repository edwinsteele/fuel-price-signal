# Handover — late-descent A mechanism diagnosed, constraint candidate filed (2026-06-07)

Copy everything below the line into a fresh Claude Code session to continue.

---

I'm picking up a multi-session investigation into the late-descent residual error budget in this fuel-price model. Today's session diagnosed the mechanism of Family A's per-fold variability at row level and filed the constraint-feature design issue. The next session is about deciding execution order, not extending the diagnostic.

## Read first (in order)

1. `experiments/2026-06-06_late_descent_triplet/step5_analysis.md` — row-level mechanism finding (opposite-signed effect on `ext_descent` vs `normal_descent`).
2. `experiments/2026-06-06_late_descent_triplet/step4_analysis.md` — fold-level negative result that motivated dropping to row resolution.
3. The new memory `project_a_dual_effect_mechanism` — the durable summary of (1) plus the leakage and shallowness sub-findings.
4. `gh issue view 212` — within-family A ablation (level / delta / both, plus C as complement). Methodology already includes mean+median + seed_std gate per `feedback_check_seed_variance_before_trusting_mean`.
5. `gh issue view 214` — shallow-elongated constraint feature (NEW, filed today). Adds `elongation_ratio` + `cycle_descent_slope_so_far` as raw axes on top of #212's winner. Blocked on #212.
6. `gh issue view 215` — planning decision (supersedes #211, also filed today). Blocked on #212 and #214.

## Memory worth re-checking

- `project_a_dual_effect_mechanism` (new today): mechanism + leakage + shallowness + fold-9 caveat.
- `project_late_descent_triplet_outcome`: step2 corrected verdict + forward-pointer to the mechanism memory.
- `project_late_descent_elongation_regime` (rewritten 2026-06-07): the elongation hypothesis history; now points at the row-level resolution.
- `project_late_descent_investigation`: broader plan; now lists the active issue chain (#212 -> #214 -> #215).
- `feedback_new_issue_not_pivot` (new today): when an issue's framing pivots, file a fresh one. Don't edit the existing body.

## State at handover

### Done today

- Step 4: fold-level 2D (elongation x gradient) diagnostic — inconclusive at fold resolution (`step4_analysis.md`, `step4_2d_panels.png`).
- Step 5: paired WFCV restricted to R0 + R5_A, per-row predictions saved (`step5_paired_wfcv_rowpreds.py`, `step5_rowpreds.parquet`, 8M rows / 32 MB). Row-level diagnostic (`step5_rowlevel_diag.py`, `step5_analysis.md`) confirmed the broad-population opposite-sign mechanism.
- Step 5c: per-fold bar decomposition showing where each fold's net A effect comes from (`step5c_perfold_bars.png`).
- Step 5d: leakage test of the `is_elongated > 1.3` threshold. Fold 9's `normal_descent` rows in [1.0, 1.3) show +0.044 mean delta_ll, almost as bad as the ext_descent bucket. Threshold is leaky on a continuous gradient (`step5d_leakage.png`).
- Step 5e: per-row shallowness split within elongated rows. Per-fold sign-flip direction confirmed on 9/13 folds. Fold 7 shallow +0.078 / steep -0.011 — clean separation for the constraint candidate (`step5e_shallowness.png`).
- Filed #214 (constraint feature design) and #215 (planning decision, replaces #211). Closed #211 as superseded.
- Memory updates + INDEX.md entry for the 2026-06-06 experiment.

### Open

- Nothing in-flight from today. All artefacts written.

### Uncommitted state

- The whole `experiments/2026-06-06_late_descent_triplet/` directory is untracked. Per `experiments/**` exemption in CLAUDE.md it can be committed directly to `main` without a PR.
- Per-dir `.gitignore` excludes the heavy regenerable artefacts: `step1_signals_sample.csv` (18 MB), `step5_rowpreds.parquet` (32 MB), `step5_rowdelta.parquet` (10 MB). Regenerate from the corresponding step scripts when needed.
- Global `*.log` rule and `__pycache__/` rule also keep the commit clean.

## Pending tasks

1. **Commit `experiments/2026-06-06_late_descent_triplet/`** to `main` directly (per lab-book exemption). One commit, no PR. Title suggestion: `experiment: late-descent triplet step5 row-level diagnostic + step4/5d/5e follow-ups`.

2. **Update `docs/PLAN_ml_signal.md`** with the step5 row-level finding and the #214 contingency in the planning decision (per #215 acceptance criteria). Light touch — point at the experiment writeup, don't duplicate it.

3. **Run #212.** The next experimental move. Implementation: copy `experiments/2026-06-06_late_descent_triplet/step2_paired_wfcv.py` into a new lab-book dir, swap the run grid for the R0 / RA_level / RA_delta / RA_both / RAC_full set (issue body has the table), keep the seed_std diagnostic + median-aggregation reporting + elongation-conditional read mandated by #212. 350 fits expected, ~18 min wall.

4. **After #212 lands and a subset graduates: run #214.** Add `elongation_ratio` (non-adaptive baseline) and `cycle_descent_slope_so_far` to `fuel_signal/features.py` first. Then run the 5-run grid in #214 with per-row predictions saved (like step5_paired_wfcv_rowpreds.py).

5. **#215 resolves** once #214 closes — the four-quadrant decision matrix is in the issue body.

## Constraints carried forward

- `feedback_no_experiment_run` is unchanged: write code, file issues, the user runs experiments and fills `experiments/results.csv`. Today was an exception because step5/step5e were pure post-hoc analysis on existing CV outputs (no model retraining) and reduced re-runs (step5 R0+R5 only) where the user was actively driving the session. For #212 and #214 (full re-trains): write the script, file/run as the user directs.
- `feedback_check_seed_variance_before_trusting_mean`: per-cell seed_std gate. Both #212 and #214 mandate it.
- `feedback_new_issue_not_pivot` (NEW today): when a planning issue's framing pivots, file fresh + close old. Don't edit existing bodies.
- `experiments/results.csv` is main-only and the user fills it — don't write from any branch.

## Where to pick up

Item (1) — commit the lab book — is the small, mechanical first move. Then either (2) the docs update if the user wants to align planning, or wait for the user to start #212.

There is no analytical question to extend this session's work. Step5's mechanism finding is the answer to "why does A regress on fold 7", and #214 is the test of "can we constrain it". Don't propose Step 6 / Step 7 follow-up diagnostics on the existing parquet without the user asking — there's a real risk of over-fitting interpretation to known folds when the right test is the new feature in a re-train.

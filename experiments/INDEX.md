# Experiments lab book

Reverse-chronological index of experiment dirs under `experiments/`. One row per dir. Keep entries terse — full write-up lives in `<dir>/README.md`.

**Status legend:** `open` (running / undecided) · `done` (concluded, no graduation) · `graduated` (code landed in `fuel_signal/`) · `abandoned` (dead end).

**Adding an entry:** copy `experiments/TEMPLATE.md` into `experiments/<YYYY-MM-DD>_<slug>/README.md`, then add a row here.

**Companion file:** [`ledger.yaml`](ledger.yaml) holds the same experiments as structured claim records (one entry per *claim tested*, not per dir), keyed on what was falsified rather than on the feature name. This file is the prose lab book; the ledger is what the feature-generator session reads. Neither is derived from the other — write both in the same pass so they cannot drift.

| Date | Name | Hypothesis | Result | Status |
|---|---|---|---|---|
| 2026-08-31 | 2026-08-31_tau_distance_of_logloss_gain | Both TGP expressions improve the WFCV log-loss screen and neither reaches the realised arbiter, whose per-fold deltas barely agree with the screen's (6/14 sign, corr -0.176). Asserted cause (`fps-e6i`): the probability improvement lands nowhere near tau=0.25, so the decision rule never consults it — "it improves a quantity the decision rule does not read". | **Falsified — and the real channel is POPULATION, not threshold distance.** Three strikes against the hypothesis. (1) **The ruler was wrong**: tau=0.25 is a CALIBRATED threshold, the screen's probabilities are RAW, and the equivalent raw threshold is **0.1136–0.1590** across folds (tau=0.25 on 14/14). Measuring `|p_raw − 0.25|` aims ~0.10 wide of the boundary, straight into the most gain-dense bin — it would have manufactured a confirmation. (2) Rows within one isotonic step of tau are 0.83% of rows carrying **0.8%** of the gain — **1.01x, exactly proportional**. The gain is threshold-*indifferent*, not threshold-remote. (3) **~4.0%** of rows change side of the boundary (sd 0.45pp over 5 seeds, flat under a ±30% `tau_raw` sweep), **232** at the arbiter's own five stations against the realised run's independently derived **423 flips / 92 decisions**. The claim survives only in the extreme tail — the top 1% by \|Δp\| sit 3x farther out (0.41 vs 0.14) but carry just ~25% of the gain. **What does separate screen from arbiter:** the screen scores 714 stations, the arbiter replays five, and the overlap is **5,693 of 798,649 rows = 0.71%**. Per-fold corr(arbiter ΔCPL, screen ΔLL) is **−0.096** paired / **−0.176** published-convention over all 714 (the latter reproducing the cited figure exactly), and **+0.478 / +0.543** restricted to the five — sign agreement 7/14 and 5/14 vs **10/14 and 11/14**. Population is the *only* change: same model, same folds, same seeds. Bootstrapped gap **+0.574, 95% CI [+0.101, +1.033], P(gap>0)=0.991**. The five stations aren't special (their per-row gain sits **0.33 SE** from the network-wide figure) — they're **141x smaller**, and at that size the effect can't clear its own fold-clustered 2·SE *even on log-loss*, which is far smoother than realised CPL. **Pipeline-validation grade: exact** — the screen's stored R0/seed-42 probabilities re-derive from the arbiter's cached baseline to **2.98e-08** (float32 epsilon; the run asserts <1e-6). **Do not close the TGP track on this** — the finding is generic to the screen/arbiter seam, so it cannot single out TGP. Two follow-ups filed: `fps-4z6` (report the screen restricted to the arbiter's replay universe) and `fps-2kc` (`aggregate.py` publishes a DIFFERENCE OF MEDIANS over paired seeds — differs by up to 0.0127 nats and flips sign on fold 2, so every correlation above is reported under both conventions). | done |
| 2026-08-28 | batch1/tgp_cycle_displacement | The retail trough is bounded below by the wholesale floor, so how far the floor has moved since this cycle's retail peak sets how deep the trough can go — a depth-remaining statistic (`tgp_cycle_displacement_cents`/`_frac_amp`) rather than a level (`station_minus_tgp_cents`, rejected) or a fixed-window velocity (`tgp_delta_7d`, inconclusive). Predicted concentration on non-shock (normal) folds, with a shock-concentrated win pre-registered as evidence AGAINST the mechanism. | **Contradicted signature, favourable headline.** Realised delta -0.2015 c/L held-τ clears batch1's single-candidate noise band in the good direction (z=-2.27 vs t=1.92) — cleaner than either prior TGP expression. But shock folds show a larger favourable delta than normal folds, the opposite of what was predicted and exactly the pattern the candidate's own signature calls evidence against its mechanism. Max per-column R² against the lock is 0.58, concentrated in the `days_since_trough_entry_*` family. **Corrected 2026-08-30 (`fps-hnp`):** regraded against the corrected empirical shock-fold set `{1,3,4,14}` (was fixed `{1,4,9,13}`) — shock -0.29 vs normal -0.16 c/L (was -0.34/-0.14); verdict unchanged, but fold 3 (not fold 13) now drives the shock number. | open |
| 2026-08-27 | batch1/stickiness_phase_saddle | `cycle_pct_through` is a network clock but a station's willingness to follow it is a per-station property (`stickiness_score`); `sticky_x_phase`/`abs_sticky_x_phase` let the model see the product a tree can only approximate via splits — predicted flips concentrated on the stickiness tails, buying earlier on high-\|stickiness\| stations, with signed/absolute SHAP columns disagreeing in orientation as the distinguishing signature. | **Inconclusive.** Realised delta -0.0735 c/L held-τ and 303 decision flips spread across 13 of 14 folds match the predicted diffuse-fold shape, but the effect sits inside batch1's noise band (z=-0.99 vs t=1.92, 80th percentile, graded against a ruler wider than this candidate's own arity). `facts.json` carries no SHAP importance/orientation data, so the candidate's own distinguishing mechanism test was never run — filed as `fps-1l1`. | open |
| 2026-08-27 | 2026-08-27_texture_icc | `placebo.TEXTURE_ICC_BOUND = 0.391` is a bound on the ICC by texture FAMILY used as a stand-in for the ICC by COLUMN, which is plausibly larger — so the shipped value is not established as conservative and wide-candidate bars may be too EASY. A bank pinned to a few source columns varies alignment alone within a column and texture between them, so a one-way ANOVA by column measures it. Per bd `fps-3jj.23`. | **MEASURED: `TEXTURE_ICC_BOUND` 0.391 -> 0.274.** 32 pinned-source draws (8 columns x 4 seeds, 5.7h) grouped BY COLUMN in a one-way ANOVA: `F(7,24) = 0.735`, `p = 0.65`, point estimate -0.071 (i.e. 0), one-sided 95% upper bound **0.274**, design resolvable to 0.262 — so it reads "could not see it", never "it is not there", and the BOUND ships, not the estimate. The family-vs-column gap is CLOSED and it closed the direction nobody could call in advance: the column ICC is SMALLER than the family bound, not larger. Three by-products. (1) **The 0.391 being replaced was not what it claimed** (`fps-8o0`, confirmed live): batch1's `network` texture family is a singleton, and `texture_channel.py` drops it from the sums of squares while keeping `n=20`, `k=5` in `df2` and `k_bar` — the published `F(3,16)=0.411 -> 0.391 / resolvable 0.359` reproduces exactly from that mix. Recomputed consistently the by-family bound is **0.226**; fixing only the filter would have RAISED it to 0.402, so both halves had to land together. (2) **The bead's own construction leans against its own assumption**: the pinned bank's alignment-only variance came out 2.2x the multi-column floor's alignment+texture variance, which an equal-`Var(alignment)` model forbids — not resolvable (ratio CI [0.90, 5.18], `p = 0.08`), so not evidence the pinned set is unrepresentative, but if real the ANOVA's ICC is biased LOW and 0.274 is optimistic. Recorded as the open edge. (3) **The 5-dead-column count is verified** — `all_nan_baseline_columns` came back with exactly 5, so every published bar table stands. **Lowering the constant LOOSENS wide bars** (arity 35: -0.287 -> -0.226 c/L); every candidate batch1 has run moved 0.001, so no dossier verdict is affected. `fps-3jj.22` **demoted, not closed** — its ceiling halved to 0.074 c/L at arity ~35, and more draws is now the cheaper route to the same tightening. Two findings still stand from before any fit was spent: the cheap one-column design could not have discharged the bead (best case 0.587, looser than what it replaced), and "it sets EVERY bar" is overstated — at arity <= 4 the constant's whole range is worth <= 0.011 c/L. | closed |
| 2026-08-26 | 2026-08-26_placebo_draw_independence | The placebo bank caps its draw count by requiring `n_draws x arity` DISTINCT source columns and the same number of entries from a fixed 30-seed pool, so the bar explodes with arity (`df = n_draws - 1`) and a 35-column per-LGA candidate is ungradeable. Is "distinct source column" actually what makes two draws independent? Per bd `fps-3jj.21`. | **No — the constraint that binds is not the constraint that matters, and the one that matters is unguarded.** Placebo-pair |rho| by what the pair shares: same column/**different** seed **0.038** median, 0.218 max; different column/different seed 0.015, 0.175; different column/**SAME** seed 0.097, **0.971**. Correlation lives in time ALIGNMENT: a different rearrangement destroys it regardless of source column, while an identical rearrangement leaves two columns' alignment TO EACH OTHER intact, so near-duplicate sources survive the shuffle. **Live in batch1's committed ruler and deterministic, not unlucky** — three primaries land on all-NaN LGA columns, the screen substitutes, and `candidate_pool`'s fallback tail restarted the seed cycle at index 0, so draw 10 reuses ALL THREE of draw 1's seeds (97/101/103) at placebo correlations 0.965/0.778/0.765. One of ten draws is a near-copy of another, on every recompute. Whole-bank: proposed 20x3 with column reuse beats current 10x3 on every statistic (median 0.012 vs 0.015, max **0.350 vs 0.965**) at DOUBLE the draws. The distinctness premise was already false — 49 usable lock columns hold 298 pairs at |rho| >= 0.7 and only 5 texture families. **Landed:** unbounded `block_seed` (first 30 values the historical pool verbatim, so <=30-pick banks stay byte-identical), `candidate_pool` lays repeated laps, `n_draws` and `arity` now independent. **Carried limitation, stated as a bound not a number:** the TEXTURE channel (two placebos from one column share distribution/NaN/autocorrelation exactly) is invisible to column correlation; ANOVA on the committed 20 deltas gives F(3,16)=0.411, p=0.75, ICC ~0 but **could not resolve below 0.359**, so the honest output is **ICC <= 0.391**, which is now PRICED into every bar rather than turned into a cap. **Superseded within the day by `fps-3jj.25`: the cap was the wrong instrument AND rested on a crude model.** It shipped at 4, was cut to 3 on review (the baseline scoring was asymmetric), then removed entirely on the owner's standing position that constraining candidate shape to suit the ruler distorts what is being measured. The modelling error: charging two draws the FULL texture ICC for sharing even ONE column put a 20-draw arity-10 bank at 2.4 effective draws; charging the shared FRACTION puts it at 9.04, and the bar then degrades GENTLY at every width (1 col -0.152, 10 cols -0.171, 35 cols -0.287 c/L) — which is exactly what the parent bead said the old design failed to do. So the cap was solving a problem the modelling invented. `placebo.effective_n_draws` now prices reuse from the bank actually built and feeds `family_wise_z_threshold`; a wider candidate gets a wider bar instead of no verdict. **The motivating 35-column per-LGA candidate builds a 20-draw bank on real data and grades at -0.287 c/L** — this bead opened by noting it could not have a band computed at all. `texture_channel.py` now re-derives the cap and fails loudly on mismatch. **High arity is NOT solved** — two 35-column draws from 49 columns must share >=21 by pigeonhole, so no sampling rule reaches it (`fps-3jj.22`). ICC measured directly in `fps-3jj.23`. Closes `fps-3jj.20`. | done |
| 2026-08-26 | batch1/lga_trough_propagation | The Sydney trough is a wave, not a moment: individual LGAs bottom on different days and the wave runs from a known set of early LGAs outward. `lga_trough_breadth_7d`, `lga_trough_breadth_delta_3d`, `lga_leader_lead_days` should recover breadth/velocity/leader-lag information that `lga_phase_std` (dispersion, sign- and order-blind) discards — predicted concentration in non-shock folds, since a shock's wholesale move overrides propagation order. | **Inconclusive on CPL; mechanism CONFIRMED as of the 2026-08-30 correction (`fps-hnp`), reversing the original `contradicted` read.** Regraded against the corrected empirical shock-fold set `{1,3,4,14}` (was fixed `{1,4,9,13}`), the pipeline's own zone test now reads `resolved: true` — target (normal) is favourable (-0.1073 c/L) and the zone predicted inert (shock) is statistically flat (+0.0026 c/L), exactly the predicted shape. Cause: fold 13, this run's single largest cell (-1.56 c/L), moves from shock to normal. Headline -0.0675 c/L held-τ (unaffected) still sits inside batch1's own noise band (z=-0.93 vs t=1.92), so this stays `inconclusive` at the CPL level. Caveat: the confirmed zone rests entirely on fold 13 — excluding it flips the normal-zone delta to +0.0588 c/L. Full account: `experiments/candidates/batch1/lga_trough_propagation/README.md` § Correction note. | open |
| 2026-08-25 | batch1/station_descent_dynamics | A station's own cut-sequence velocity/deceleration (`station_px_change_3d`, `station_px_change_14d`, `station_descent_decel`) should let the model read "this station has arrived at its floor" from its own pace collapsing, not just the network clock — predicted concentration in the elongated-cycle folds (3, 7), modest concentrated flip count. | **Inconclusive — and the specific mechanism is contradicted.** The pipeline's own zone test on the candidate's declared target (folds 3, 7) reads `resolved: false` — those folds WORSEN slightly (+0.0138 c/L) while the rest of the run carries the improvement (-0.0345 c/L); flip count is large and diffuse (439 across all 14 folds) not modest and concentrated. This zone verdict is unaffected by the shock-fold correction below (TARGET grades folds 3/7 directly, not regime). None of this reaches the arbiter — headline -0.0272 c/L held-τ sits well inside batch1's own noise band (z=-0.52 vs t=1.92, 60th percentile of noise draws). **Corrected 2026-08-30 (`fps-hnp`):** the descriptive per-regime framing flips sign — against the corrected empirical shock-fold set `{1,3,4,14}`, what improvement exists now traces to NORMAL-regime folds (shock +0.0703 vs normal -0.0720 c/L; was shock -0.2837/normal +0.0706), because fold 13 moves from shock to normal. **Post-hoc addendum 2026-08-25 (not pipeline-generated, `posthoc_decel_conditional.py`):** the columns are NOT inert — within steep descents (`px_change_14d ≤ -15`), `station_descent_decel` separates 3-day restoration-cliff risk 2.22% (cuts still accelerating) vs 6.6–6.8% (descent stalled) against a 5.37% base rate, on 714 stations / 770,799 rows, in the predicted direction. But the edge is ~0.67 c/L per firing arriving in ~7% of firings — a fat-tailed shape a 14-fold pooled-CPL arbiter cannot resolve. Fold 13's -1.28 c/L is that tail sampled twice, amplified by R0's tank running dry inside both plateaus (56% of the fold's litres bought at a 19.4 c/L forced-fill premium). Next move is the INSTRUMENT, not the hypothesis. | done |
| 2026-08-24 | batch1/network_move_breadth | Cross-sectional directional participation counts (`network_rise_breadth_3d`, `network_fall_breadth_3d`, `network_rise_breadth_delta_2d`) should let the model see a coordinated end-of-cycle restoration starting (peers jumping up) and flip WAIT→BUY early, in a few large decision flips concentrated on shock folds — where `network_px_std`'s direction-blind spread can't. | **Inconclusive — revised 2026-08-24 with `fps-gez`'s decision-flip data.** The favourable effect is real and sustained (3 consecutive folds, 11–13, each -12 to -14.5 c/L flip-only CPL), not one lucky fold. Originally read as straddling the shock/normal boundary (only fold 13 of the three was `SHOCK_FOLDS` under the old fixed set). **Corrected 2026-08-30 (`fps-hnp`):** against the corrected empirical shock-fold set `{1,3,4,14}`, fold 13 moves to `normal` — ALL THREE of folds 11-13 are now `normal`, sharpening rather than resolving the calendar-vs-regime contradiction. Zone `resolved` stays true (shock -0.21 vs normal -0.13 c/L, was -0.31/-0.09), but now because fold 3 (not fold 13) drives ~74% of the shock-zone effect on its own. Headline realised delta (-0.1552 c/L held-τ, unaffected) still sits inside batch1's single-candidate noise band (z=-1.80 vs t=1.92, arity-3 floor). | open |
| 2026-08-22 | 2026-08-22_placebo_block_sizing | Make the two MEASURED constants behind `placebo.py`'s block-permutation placebo re-derivable (`fps-d7m`): `PLACEBO_BLOCK_DAYS`=61 and `MIN_BLOCKS`=24 were justified by tables that existed only in a docstring, with the generating scripts in a scratch dir. Plus fps-d7m's acceptance-criterion-2 batch0 screen. | **Both constants reproduce; scripts now assert against the shipped values.** **Table 1 (block COUNT, 500 seeds, adversarial drift-dominated probe) sets MIN_BLOCKS:** worst-case |self-corr| by count — 8→0.792, 12→0.751, 16→0.740, 20→0.657, **24→0.555 (first to clear the 0.6 gate)**, 30→0.529, 59→0.332. At 12 the p99 is 0.652, i.e. ~1 draw in 100 fails and gets substituted away — and it would be a LEVEL-LIKE column, the exact exclusion fps-d7m removes, arriving by another route. Binds only below 24×61=1464 positions (never on batch0's market-wide branch; ~thinnest tenth of stations). **Table 2 (block LENGTH) sets PLACEBO_BLOCK_DAYS and shows it's a CHOICE, not an optimum** — lag-1 autocorr / median self-corr: 14d 0.927/0.039, 29d 0.962/0.052, **61d 0.977/0.083**, 127d 0.990/0.124, 251d 0.993/0.158 (original 0.999). Shorter decorrelates better but preserves less texture; 61 clears one whole cycle (batch0 `cycle_mean_length` mean 30.5/max 35.3) while leaving ~58 blocks. **Table 3 (real batch0, shipped code):** 5 all-NaN skipped, 49 usable × 5 seeds = 245 checks, **0 correlation failures**, median-of-max 0.046, worst 0.470. **Two skip reasons kept separate because conflating them was a real error in this work's own prose (review-caught): correlation-driven substitution NEVER fires under block permutation (it fired on 8 columns every run under the circular shift); all-NaN substitution fires TWICE every run** (`..._lane_cove` seed 157, `..._waverley` seed 181 → `cycle_days_since_peak`, `cycle_mean_length`), and the published bank composition is only reached because of them. **Carried-forward limitation:** a mostly-cross-sectional column resists ANY time-axis reorder — `stickiness_score` 0.470, `station_minus_sydney_avg_cents` 0.328 vs a 0.046 median; they pass and enter the bank as its weakest draws, and the gate is deliberately NOT tightened to exclude them (that would re-create the exclusion along the station axis instead of the level axis). **Correlation only — the c/L cost is settled by `noise_floor.py <batch> --force`, still outstanding (`fps-cds`).** | graduated |
| 2026-08-21 | 2026-08-21_tau_cadence | Is the daily-cadence plateau fps-fii found (model gains 1.82 c/L going 7d→2d, then 0.03 going 2d→1d, while the oracle keeps improving) an OPERATING-POINT limit or a SIGNAL-CONTENT limit? τ=0.25 was OOF-selected under weekly economics where 67.6% of fills are forced; a model with ~12 wait-rungs instead of 1 should plausibly be choosier. Sweep 12 τ × 3 cadences off ONE paid walk-forward fit (the fit is both tank- and τ-invariant), with a per-CELL run-dry audit since a higher τ can strand a tank a lower one kept wet. Per bd `fps-929`. | **SIGNAL-CONTENT LIMIT — the operating-point explanation is ruled out.** Best-τ-in-hindsight beats production τ=0.25 by **0.009 c/L at 7d, 0.086 at 2d, 0.062 at 1d** — 2% of the 2.97 c/L available at daily cadence. The hypothesis is directionally right and economically irrelevant: the optimum does climb 0.20→0.30→0.35 as the grid refines, and it is worth nothing. **The decisive line is the flip count, and it inverts the usual reading**: at 1d, moving τ 0.25→0.35 changes **422 buy/wait decisions** and moves pooled CPL 0.062 c/L — the changes cancel, so this is a real measurement of no effect, not an underpowered measurement of a real one (at 7d the same comparison moves 8 decisions). Plateau width over τ∈[0.15,0.50] narrows 1.061→0.461→**0.318** c/L as cadence refines. Flatness is directional: above the optimum 1d is far flatter than 7d (0.21 vs 1.06 at τ=0.50), below it the reverse (1.01 vs 0.13 at τ=0.05) — only the upper side answers the question. All 36 cells dry-free. Self-check: every fold's OOF τ = 0.25, and 7d/τ=0.25 reproduces fps-fii's 189.6717. Sweep picks τ in hindsight, so a flat curve RULES OUT the operating point while a dip would only have upper-bounded a gain — the definitive half is the half that applies. **No τ re-lock; STATUS.md unchanged.** Cadence itself is untouched and much larger (1.85 c/L cheaper at 1d, but 2332 fills vs 752 — 3× the servo visits, a cost CPL does not price). **Addendum 2026-08-22: cadence is no longer untouched — `fps-oqz` re-locked it 7d → 1d, on the owner's rationale (uniformity, resolution, a choice every day) rather than on this row's economics; the 2d option this session recommended on the hassle trade-off was considered and overridden. τ stays 0.25, as this row concluded. The canonical cadence is now 1d, so this row's "production 7-day" framing is historical.** | Done |
| 2026-08-21 | 2026-08-21_path_coupling_audit | Repo-wide audit (bd `fps-grp`): the `2026-08-20_headroom_attribution` defect is a property of the QUANTITY (a path-coupled total allocated to a sub-period has no unique answer), not of the #262 script — so what else in this repo computes the same shape and inherits it silently? No new runs: every verdict is structural (read off the producing code) or arithmetic on committed numbers. | **6 at risk, 10 safe — and the two gate1 verdicts are decided: WITHDRAWN.** The pivotal structural fact, verified not assumed: **a fold is not a sub-period** — `realised.py` calls `aggregate_backtest` once per fold and `aggregate_backtest` calls `run_backtest` once per station with a fresh tank, so per-fold/per-station/per-fold-group economics are sums of COMPLETE WINDOWS and are safe, while row-label cuts are not. **A1/A2 — the 2026-06-18 gate1 per-regime saving% is NOT IDENTIFIED, in both directions** (Layer-2 gradient 0.64/2.13/4.06 AND Layer-3 chosen-only 11.45/6.45/11.03 + flat CPL 174.5/176.4/174.9). Demonstrated on committed numbers alone: emergency-fill inclusion is a free convention (Layer 2 includes, Layer 3 excludes; both defensible) and is the path-coupling carrier in its purest form — an emergency fill stamped in `normal` settles a wait chosen elsewhere. Spread ACROSS conventions 21.30/8.15/13.69 c/L vs BETWEEN buckets 6.73 (all) / 10.40 (chosen); **the ordering reverses**, best→worst `overdue,late_descent,normal` → `normal,overdue,late_descent`. Worse coupled than #262's: the arms' denominators are wholly unpaired (always-buy fills every eval date) and `realised_by_regime.csv` records no `always_litres`, so pairing isn't even checkable. **Silent by construction** — measured vs always-buy, so no impossible negatives to give it away. **Layer-1 proxy regret (1.05/1.72/1.32) is SAFE** — a per-row TP/FP/FN score, no tank, no path. **Live/prospective risk found in the AI-feature pipeline:** `per_axis` deltas + `CONFIDENCE_ZONE` grading on a candidate `add_axis` row label (not yet triggered — batch0 declared none — but wired in and it would feed the cross-batch calibration record), and `docs/routines/generator.md` was still quoting #262's withdrawn `12–16c 7.09` and "regime axis FLAT" **verbatim as standing guidance to every future candidate**. Also safe, confirmed rather than assumed: results.csv (all 6 realised rows are one window, 2025-07-01→2025-12-31), `per_fold`/`per_regime`(SHOCK_FOLDS), evaluate.py, score_phase2.py. **Corollary added: cut economics on folds, not on row labels.** Dissolves `fps-x0f`'s three-way contradiction — the two findings that disagreed are both now withdrawn, leaving one un-contradicted proxy reading and no identified realised measurement of the regime axis at all. | done |
| 2026-08-20 | 2026-08-20_cadence_ceiling | Is #262's window-level 1.66 c/L headroom an artifact of the 7-day decision cadence (`TankParams.evaluation_interval_days`, a default rather than a fact about the world)? Re-run the fit-free oracle AND the model arm at finer cadences over the same 14 windows / 5 stations / seed / columns, so headroom is like-for-like. Per bd `fps-fii`. | **The 7-day grid was hiding ~half the prize: headroom 1.54 -> 2.97 c/L at daily cadence** (7d always 193.41 / model 189.67 / oracle 188.14; 1d 192.99 / 187.82 / 184.85). Robust — grew in 12/14 folds, median +1.70. **The real finding is WHERE it grows: the model plateaus and the oracle does not.** Model improves 14/14 folds 7d->1d but stalls at 189.67 -> 187.85 (2d) -> 187.82 (1d) — 0.03 c/L for doubling its chances — while the oracle keeps going; captured share falls monotonically 71% -> 65% -> 64%. So at 7 days the TANK was the binding constraint and at 1 day the SIGNAL is. **Forced-fill count is cadence-INVARIANT** (508/499/500) because hitting the floor depends on distance driven, not on how often you look — chosen fills go 244 -> 896 -> **1832**, so the "~250 real decisions, hence structurally small feature effects" explanation (and the ~0.05 c/L flip quantum) is a 7-DAY quantity, not a property of the tank. **Engine defect surfaced:** `run_backtest` clamps a dry tank to 0 while `run_oracle_backtest` prunes run-dry paths, so the oracle is only a ceiling over never-dry strategies; at 3d a reachable level (7.14 L) sits above the 10% floor yet cannot survive a 10.71 L interval -> 109 dry events / 389 L, headroom meaningless, NOT QUOTED. Exact rational arithmetic: cadences 1/2/7 safe, 3/4/5/6/8-14 not. **#262's 189.79 does not reproduce** — always-buy and oracle match to the cent (price-only) but the model comes in at 189.67, so 7d headroom is 1.54 not 1.66 (~2-3 decision flips, features.csv vintage). Leaky ceiling and window-level-only caveats unchanged. Next: re-pick tau for daily operation before ANY feature work — tau was held at the production 0.25 throughout, so the daily arm is the production model operated as-is, not tuned for 7x more decisions. | done |
| 2026-08-20 | 2026-08-20_headroom_attribution | Fix the #262 per-zone headroom estimator (bd `fps-1785999730023-4-264564ac`): re-attribute cost to CONSUMPTION intervals rather than purchase events so both arms carry identical per-zone denominators, and put station/fold bootstrap CIs on every cell. Expected: the impossible negatives (a perfect-foresight oracle cannot lose) disappear and the cells become trustworthy enough to drive feature decisions. | **The estimator fix works; the quantity it estimates is NOT IDENTIFIED.** Denominators are now paired (0.0-1.7% litre difference per zone vs ~30% fill-count differences), the replay reconciles exactly on all 140 station-fold-arms, and the recomputed old estimator reproduces #262's published 7.09 / -0.53 to the decimal. But per-zone headroom depends on TWO free bookkeeping conventions with no physical content — cost basis (fifo/average: which litre you burn first) and interval label (start/mid/end: which date stamps a burn spanning [d,d+7)) — and across those 6 combinations each zone moves MORE than the zones differ from each other. Regime: per-zone spread 2.53-2.95 c/L vs between-zone 0.90-3.36; the 'worst zone' changes three times. Volatility is WORSE not better: `>=16c` ranges -0.11 to 5.02 (spread 5.13). **No contrast separates on either axis** (best: late_descent>normal p=0.90, CI [-0.77,+3.49]; 12-16c vs >=16c p=0.42, a coin flip). Impossible negatives are not robustly fixed either — they vanish at `start` labelling but return at average/end and average/mid (3 negative cells across the 42-cell grid). **Rule: a zone comparison is readable only when the gap between zones exceeds the gap between conventions.** SURVIVES: window-level headroom (1.66 c/L, no allocation needed, `oracle <= model` holds by construction) and `<8c` volatility (0.96-1.13, spread 0.16 — calm periods have little path coupling, the exception proving the rule). **WITHDRAWN from #262: every per-zone row** — 'regime axis FLAT' (which rested the late-descent thread), the 12-16c hump's magnitude and peak status, monthly episode clustering, and the +/-0.5 per-band noise floor derived from the negatives. Not a variance problem: more stations/folds/seeds shrink error bars and do nothing to convention spread. Redirects `fps-x0f` step 2 to a measure that never allocates cost to a period. | done |
| 2026-08-19 | batch0/tgp_delta_7d | **Pipeline calibration**, not a verdict on the feature: does the AI-sourced pipeline reproduce an independently written script on identical inputs? (Supersedes the 2026-08-18 run — same candidate, wrong baseline; see the dossier's § History.) | **CALIBRATION PASSED.** Pipeline returned `delta_cpl_held` **+0.005851** against an independent reference of **+0.0059** (`diagnostic_baseline_composition.py`) — R0 189.6717090069284, candidate 189.6775604142693, matching to every printed digit. The reference predates the fix that made the run possible, uses a different script and a different injection path (`.asof` vs `add_columns` + exact-key, 822/88), so those 88 misses again never touch a live decision. **The pipeline is exact; what was wrong twice was the contract handed to it.** Took three attempts: (1) 64 cols — `resolve_baseline_columns()` discovered the 10 Phase 4b brand-trough columns **rejected 2026-06-02** → +0.0941 (fps-sa1, PR #309); (2) 54 cols **sorted** while the locked artifact is group order, differing in 52/54 positions — LightGBM breaks equal-gain split ties by feature index → +0.0439 (fps-zci, PR #310); (3) 54 cols, production order → **+0.0059**. **On the feature: inert, and never measurable.** Per-fold pooled contributions span −0.209…+0.654 c/L for a +0.006 total; arms differ on 11/752 fills; **one decision flip ≈ 0.05 c/L pooled — larger than the −0.039 that graduated this feature in June**, which does not reproduce. Wrong set (+0.088), data vintage (+0.045) and column order (+0.038) each moved the headline by about one flip. WFCV log-loss still favours the candidate (Δll_all −0.0137, hard25 −0.0646). Fold 13's old +1.09 dominance was a baseline artifact (+0.127 here). **Noise floor (fps-3jj.9, run against batch0 2026-08-19): candidate sits at the 40th percentile of pure fit noise** (band mean −0.0135, std 0.142 c/L; z=+0.14) — confirms inert. #271 stays on **hold**. | done |
| 2026-08-10 | 2026-08-10_backfill_requery_profile | `classify_range` + `score_leadership_range` are seed-db.yml's two heaviest steps (~25 of 46 min on a GH runner, worse on Viking's APU3) because both re-query SQLite once per snapshot over a trailing window overlapping the previous one by ~98%. Expected: redundant re-query dominates, fix is data-access not algorithm. | **CONFIRMED, and more lopsided than expected — SQL is 79% of classify's read+compute and 99% of leadership's** (scipy/trough detection is 1%). Per-snap: classify SQL 39ms / compute 11ms / write 5ms / commit 8–18ms; leadership SQL 1042ms / compute 12ms. Mac→GHA ratio 1.66–1.75x, so projections carry. **Memory forces two different fixes:** full-range preload is 30 MB for `_load_lga_sums` (SQL already aggregates to (date,LGA), 106k rows) but **540 MB resident / 881 MB peak** for raw per-station `daily_prices_in_window` (2.23M rows) — the latter would OOM Viking. So leadership → preload + `searchsorted` slice; classify → one `ORDER BY price_date` scan (free: index already delivers that order, no sort step) feeding a 45-day deque of per-day buckets, ~7 MB resident, yielding buckets not flattened rows. Prototypes byte-identical to current queries: classify read+compute 198s → ~16s (12x), leadership read 497s → 5.4s (92x). Leadership parity is *structural* — `HAVING` groups on (price_date, council), every group inside one date, so no window can split one. Projected GHA: classify 471s → ~90s, leadership 1019s → ~40s; the two steps 25min → ~2min, seed-db.yml 46min → ~23min (history.py's network-bound 16min becomes the bottleneck). Convention in AGENTS.md § 'Backfill (--start-date) paths'. Implementation split into fps-53j (leadership, first) + fps-2us (classify). | done |
| 2026-06-20 | 2026-06-20_leading_indicators | Exploratory (#215): find a free, daily-pullable external upstream series that leads Sydney E10, view it side-by-side over history, and decide if there's exploitable lead → candidate feature. | **Source found: AIP Sydney ULP TGP** (free xlsx, daily weekday 2004→, c/L GST-incl — the wholesale floor). **Overlay:** TGP is the floor, cycle troughs "kiss" it (kiss-gap mean −0.3 / sd 3.1 c/L); lead is contemporaneous in calm, multi-week in shocks. **Oracle diagnostic:** gap→depth-remaining r²=0.63, gap→days r²=0.47, **gap÷slope r²=0.19 (worse — ship raw gap not the time projection).** **Gap screen:** `station_minus_tgp_cents` helps calm (Δll_all −0.012) but HURTS shock (+0.011) — inverse of hypothesis; mechanism = today's TGP misleads when the floor is *moving*. **Velocity redesign:** `tgp_delta_7d` carries independent signal (vel-only normal −0.018 / shock −0.010) and RESCUES shock; raw gap stays a shock drag, explicit interaction marginal+seed-unstable (dropped). **Horizon sweep:** **7d is Goldilocks** (3d too jumpy for the sustained shock move, 14d too sluggish for the cycle). Candidate = `tgp_delta_7d`; clears the log-loss screen incl. the #262 shock regime. **Realised/CpL arbiter (14-fold WF, in-process inject via #269 seam, all arms τ=0.25 so pure feature effect): `tgp_delta_7d` GRADUATES** (pooled Δcpl −0.039 c/L, gains concentrated in the expensive 200+ c/L folds 8/9/10 where #262 headroom lives) — **RETRACTED 2026-08-19: this graduation does not hold. −0.039 does not reproduce (+0.0059 on identical columns/folds/seed) and is smaller than the ~0.05 c/L single-decision-flip quantum; against the noise floor it sits at the 40th percentile of pure fit noise. No TGP feature has graduated. See the 2026-08-19 batch0 row.**; **raw gap `station_minus_tgp_cents` does NOT** (gap_vel7 pools better at −0.136 but by the wrong trade — rescues low-headroom calm folds 5/13 while zeroing vel7's big wins in expensive folds 9/10; the floor-proximity brake cancels velocity's pre-spike buy). Folds 9/10 equal-and-opposite = ONE cancelled marginal flip, not a bug. Ship vel7 alone → production path #271. hard25 de-emphasised. | done |
| 2026-06-19 | 2026-06-19_headroom_map | Economic headroom map (#262): where (if anywhere) does recoverable value concentrate, measured as `model_cpl − oracle_cpl` (perfect-foresight `run_oracle_backtest` ceiling under the same tank dynamics, NOT vs always-buy), sliced by ≥3 zone-agnostic axes (cycle regime / quarter / volatility / fold)? Successor to #259 (Gate-2 moot — no late-descent deficit). | **Overall headroom 1.66 c/L** (model 189.79 / oracle 188.14) — model already ~⅔ of the way from always-buy to the oracle floor. **Regime axis FLAT** (late_descent 1.00 / normal 1.50 / overdue 1.33) → independent confirmation of #259; late-descent/cycle-phase thread RESTS. **Volatility is the only live axis, and a HUMP not a ramp**: re-cut on fixed `network_px_std` cent-bands (post-hoc), headroom <8c −0.07 / 8–12c 2.67 / **12–16c 7.09** / ≥16c −0.53 (tercile cut's tidy 0→2→3.6 was a binning artifact). Negatives calibrate per-band noise floor ≈±0.5 c/L. **Episodes cluster on macro crude/wholesale shocks** not the dispersion metric (biggest month 2022-05 = 13 c/L at only 10c vol; robust contiguous 2023-08+09 ≈5.5; monthly noise floor ≈±3 c/L). Leaky ceiling: 7 c/L is an upper bound on a largely-unforecastable prize (model already sees `network_px_std`; the gap is "which way it breaks"). **Surviving lead = external wholesale/crude (#215)**, not cycle features; signal-from-cycle in diminishing returns. Any feature must clear the realised arbiter. — **PER-ZONE ROWS WITHDRAWN 2026-08-20 (see the 2026-08-20_headroom_attribution row). `model_cpl − oracle_cpl` is not identified once sliced by any time zone: free bookkeeping conventions move each zone 2.5–5.1 c/L while the zones differ by 0.5–3.4, no contrast separates on either axis, and the impossible negatives return under 2 of 6 conventions. So 'Regime axis FLAT' (which rested the late-descent thread), the 12–16c hump's magnitude AND peak status, the monthly episode clustering, and the ±0.5 per-band noise floor derived from the negatives are all withdrawn. The WINDOW-LEVEL rows STAND — overall headroom 1.66 c/L, model ~⅔ of the way from always-buy to the oracle floor — as does the leaky-ceiling caveat. Note the 'surviving lead = external' conclusion drew partly on the now-withdrawn flat regime axis; its other support (#214/#231, log-loss based) is unaffected. Reopened as bd `fps-x0f`. Re-confirmed and cross-referenced 2026-08-21 by the 2026-08-21_path_coupling_audit row (bd `fps-grp`), which also added the withdrawal banner to this dir's own README — until then only this INDEX row carried it. Two further points that row makes: the surviving window-level 1.66 is cadence-conditional (1.54 @7d on the current features.csv vintage, 2.97 @1d — quote it with its cadence), and `docs/routines/generator.md` was still handing the withdrawn per-zone rows to the feature generator verbatim as standing guidance.** | done (per-zone withdrawn) |
| 2026-06-18 | 2026-06-18_late_descent_gate1 | Is realised buyer economics materially worse in the late-descent / elongated zone than in normal rows (#259 gate 1)? Regime tag = `cycle_pct_through` three-band (normal <0.6 / late_descent 0.6–1.0 / overdue ≥1.0). Layer 1 = cheap proxy pre-read (per-regime regret = oracle ceiling − peak `expected_cents_per_row`, off #254 R0 rowpreds, zero new fits); Layer 2 = realised CPL via a per-fill ledger on `run_backtest`. | **Gate 1 FIRES (proxy + realised agree).** Layer-1 proxy regret: normal 1.05 / late_descent 1.72 / overdue 1.32. Layer-2 realised saving% vs regime-matched always-buy (14-fold WF, PR #261 ledger): normal 0.64% / late_descent 2.13% / overdue 4.06% (pooled 1.87% @ τ=0.25 = prod lock). Monotonic gradient confirms regime is economically LIVE; overdue ≈ 2× late_descent on real spend (matches proxy 42% vs 19% capture). **Reframe:** overdue/elongated is the model's BEST zone (easy deep troughs), NOT the hypothesised worst — soft spot is late descent (ambiguous descent into trough). saving% is vs always-buy (weak baseline) so it's value-delivered not recoverable headroom → recoverability = Gate 2's oracle job. **Layer 3 (cleanup checks, run since) OVERTURNED that reframe** — chosen-only saving% is 11.45/6.45/11.03 and chosen-only CPL is flat at 174.5/176.4/174.9, so the gradient was emergency dilution (normal is 87.5% forced) plus a regime-varying always-buy denominator; and the 9.3% pct-join-miss drops are 100% overdue, censoring ~34% of overdue fills at the extreme-elongation tail. — **ALL PER-REGIME REALISED FIGURES WITHDRAWN 2026-08-21 (see the 2026-08-21_path_coupling_audit row). Both directions go — the Layer-2 gradient AND the Layer-3 flat-chosen-only reading.** Realised CPL is a tank-path total tagged by the regime at each fill's own date; allocating it to a sub-period has no unique answer. Emergency-fill inclusion is itself a free bookkeeping convention (and the path-coupling carrier — an emergency fill stamped in `normal` settles a wait chosen elsewhere): across it each bucket moves 21.30/8.15/13.69 c/L while the buckets differ by only 6.73–10.40, and best↔worst swap places. Layer 3 is NOT the corrected Layer 2, it is a second draw from the same unidentified quantity. Measured vs always-buy, so no impossible negatives to expose it — **silent**. **STANDS:** Layer-1 proxy regret 1.05/1.72/1.32 (per-row TP/FP/FN score — no tank, no path), the Layer-3 *diagnosis* of what drove the gradient (strengthened), the overdue drop censor, and "always-buy is the wrong yardstick". **Net: the realised regime axis has never been measured with an identified instrument, in either direction.** Successor #262 (per-zone rows also withdrawn); thread reopened as bd `fps-x0f`, whose three-way contradiction this collapses. | done (per-regime realised withdrawn) |
| 2026-06-16 | 2026-06-16_regime_cycle_length | Does swapping `cycle_mean_length`'s expanding all-history mean for a regime-local, COVID-break-floored shrunk median (k=2, pre-COVID-median prior, cycles stamped at closing peak) improve the model? Cycle features recomputed live through a patched `cycle.py`. Arms: baseline / +regime denom / +`is_post_covid` dummy + paired realised backtest. Per #254. | **ABANDONED — no graduation.** Regime denominator is genuinely more accurate (validate.py: ~27d→38d, removes ~1.15× fold-7-era `pct_through` inflation) but does NOT buy realised value. WFCV: flat/seed-fragile, fold 7 +0.009 (and the fold-7 gate was mis-specified — WFCV is the wrong arbiter for a trough-timing feature; #250 precedent). Realised backtest (2025-H2): regime @ its own τ=0.20 = 184.75 c/L / +3.66% looked like a win, but **at held τ=0.25 it's 185.83 vs lock 185.32 — 0.51 c/L WORSE**. The apparent win was the τ move (0.25→0.20 ≈ 1.08 c/L), not the feature; operating point dominates the feature ~2×. **Deep finding: accuracy ≠ objective — a more accurate (larger) cycle length gave worse CPL** because the model wants a decision variable, not a ruler, and the expanding mean's errors (trough-timing hedge / regime clock) are economically informative. Inverts the #237/#253 "broken ruler" premise. | done |
| 2026-06-14 | 2026-06-14_corner_oracle_sweep | Does any OTHER intra-series signal (7 candidates across 3 families: price kinematics, cross-LGA/brand trough consensus, discount-cohort lead) carry the shallow-vs-steep-within-elongated distinction that A doesn't — read on a train-only oracle existence check? Per #237. | **PARKED — results invalid, do not read.** Two defects surfaced *during* setup and contaminate the phase axis the sweep is built on: (1) `cycle_days_since_peak` whipsaws at boundaries (#250) → dsp-reset cycle reconstruction over-segments ~2.6× vs find_peaks; (2) `cycle_mean_length` is an expanding all-history mean → lags the early-2020 ~24d→~38d regime shift → `cycle_pct_through` (the x-axis) inflated ~13% (worse in fold 7's era). Reframes the premise: #214/#231 rejected A on this same broken ruler, so "A exhausted" is no longer safe. Durable artifact: `regime_cycle_length.{py,png}` (evidence for #250 + regime fix). Rebuild on find_peaks + clipped-at-break regime-local axis, A re-included. **Update 2026-06-15: defect 1 (#250) FIXED + merged (PR #251, sticky find_peaks confirmation) — the find_peaks-boundary half of the rebuild is unblocked once features.csv is regenerated. Defect 2 (cycle_mean_length regime-local axis) still open.** | open (parked) |
| 2026-06-11 | 2026-06-11_interaction_column | Does handing the tree an explicit interaction column (`A × is_extended_shallow_descent`, complement, or smooth `A × elongation × max(0, slope+0.9)`) close the fold-7 regression that R_raw (#214) couldn't — testing whether the failure is combination representation vs underlying signal availability? Per #231. | **REJECTED.** All 3 runs fail gates 1+2; fold 7 got *worse* (R1/R3 Δh25 ≈ +0.04, R2 +0.018), not better. Answers the discriminator: it's signal-availability, not representation — A's reading in the corner is actively misleading, so isolating it (`A_x_shallow_elong`) concentrates the harm and the greedy tree backfires on it. Confirms #214's oracle diagnostic. Seed-variance clean on fold 7 (only shock fold 4 flagged). Intra-series rehab of A in the corner is exhausted (#214+#231). Next: oracle sweep over *other* intra-series candidates before external data — not A. | done |
| 2026-06-09 | 2026-06-09_shallow_elongated | Do two-axis (`elongation_ratio`, `cycle_descent_slope_so_far`) features close the fold-7 ext_descent_shallow regression that A's `network_px_std` misreads — without giving back the lift A buys on normal-descent rows? Per #214. | **REJECTED.** R_raw fails gates 1+2 (fold 7 worsens marginally, worst-fold opens to +0.034). R_composite is null. Oracle diagnostic on train-only segment: A's level separates elongated vs normal from phase 0.2, but **shallow vs steep within elongated** is not visible in A until phase >1.4 — and per step5e the failure regime is specifically shallow-elongated, not elongated broadly. Steep elongated rows don't need different treatment. Ingredients exist (slope + A) but the tree couldn't combine them through nested splits. Recommended next probe: explicit interaction column `A × is_extended_shallow_descent` (one split instead of three). Memory + #215 updated. | done |
| 2026-06-09 | 2026-06-09_lgbm_speed_bench | Does `force_col_wise=True` (C1) or `lgb.train` + Dataset reuse (C2) cut per-fit time ≥10% vs LGBMClassifier baseline (C0) at identical log-loss? Per #220. | **Both SKIP.** C1 passes equivalence but is 69% *slower* (col-wise crossover needs ~100+ features; row-wise was already optimal at 54). C2 fails equivalence — manual param dict diverges from LGBMClassifier defaults, fold 4/seed 46 produces 0.82 ll vs 0.42 (near-degenerate); 9% speedup marginal even if fixed. C0 unchanged. Closes #220. | done |
| 2026-06-08 | 2026-06-08_dispersion_cohort_band | Does the `network_px_std` family run better under the canonical Competitive cohort (`sc.class = 'Competitive'`, ±10c) than the current ±5c filter inherited from #212? Per #219. | **SWITCH_TO_10C.** Paired CV, 14 folds, 5 seeds, additive on 50-feat baseline. R2 (±10c) Δh25 median = −0.0386 (10/14 helps); R1 (±5c, current) = −0.0299 (8/14). Margin −0.0088, outside the +0.005 parsimony tolerance. R2 wins on all four cohorts (all/h25/h10/lated); mean-agg agrees; seed-variance gate clean on h25. Per-fold: canonical cohort **amplifies, doesn't smooth** — helps and hurts both grow. Fold 7 (ext_descent_shallow, the #214 failure mode) goes from R1 +0.090 to R2 +0.124. Cohort sizes: ±5c=1.60M rows, ±10c=1.88M (1.17×, not 2× as expected). Implementation in #221. Closes #219. | graduated |
| 2026-06-07 | 2026-06-07_a_c_ablation | Within-family A ablation (RA_level / RA_delta / RA_both) + does C add an increment on top of RA_both. Decides which subset of A graduates to `fuel_signal/features.py` and whether C graduates alongside. Per #212. | **RAC_full graduates (4 cols).** Median-across-seeds, 14-fold paired CV: RAC_full Δh25 = −0.045, helps 10/14. RA_level alone = −0.032 (best A subset by parsimony); RAC_full beats it by +0.013 (clears C's +0.010 gate). `network_px_std_delta_3d` kept despite RA_both ≈ RA_level on aggregate — it's regime-sensitive and materially helps fold 7 (the #214 failure mode). Elongation r = −0.21 to −0.32 across runs (more elongation → more help) — opposite of `project_late_descent_elongation_regime` hypothesis; fold-level elongation framing retired. Implementation in #216. #215 still blocked on #214. | graduated |
| 2026-06-06 | 2026-06-06_late_descent_triplet | Three intra-series mechanisms (A: cross-station px dispersion, B: discount-cohort gap, C: LGA leader divergence) targeting the late-descent residual error budget from #206. | **A graduates; B drops; C complementary.** Step 2 corrected verdict (median-across-seeds): A standalone hard25 +0.030 / lated +0.017; B flat; C +0.014 / +0.018. Step 5 (2026-06-07 row-level): A's effect is **opposite-signed** on `ext_descent` (+0.017 mean, +0.043 hard25) vs `normal_descent` (-0.009 / -0.042) — nearly cancels in aggregate. Step 5e (shallowness split): per-fold sign-flip; constraint candidate `elongation_ratio` + `cycle_descent_slope` filed as #214. Fold 9 unrescued (back-to-back-elongation breaks adaptive baseline; out-of-scope side-quest). Planning decision in #215. | open |
| 2026-06-05 | 2026-06-05_phase_lookup_nonparametric | Replace step-4's linear-interp `expected = last_min + pct × (last_max − last_min)` with a non-parametric per-fold `E[norm_price \| phase]` lookup; if fold 1 still regresses the diagonal-projection diagnosis is confirmed, if it flips the shape misspecification was the real issue | **Phase-residual concept retired.** Additive (51 features) neutral (median Δ −0.0004); ablationA (49, drop siblings + add lookup) regresses (median Δ +0.0046, fold 9 worst +0.066 — improved from predecessor's +0.101, so shape correction was real). Methodology lesson: pre-committed macro-event shock taxonomy was carrying fold 9 specifically; re-aggregation by empirical ll_baseline showed fold 9 is mid-pack difficulty, not a shock. Mechanism: late-descent × cycle-elongation × lookup-tail-noise triple-stack, not generic shock fragility. Followup #206: late-descent feature investigation. | done |
| 2026-06-04 | 2026-06-04_cycle_pct_through_interaction | `cycle_pct_through` × cents features show ~10% partner-score plateau and clean SHAP saddles; engineering `station_price − interp(trough,peak,pct_through)` should absorb the implicit interaction and let us drop the siblings | Step 1: partner-score conflates substitution + true interaction (`station_minus_sydney_avg_cents` is the redundancy candidate, `station_minus_last_min_cents` the genuine saddle). Steps 2–3 single val window: additive neutral, ablationA looked good (Δ −0.010, mean\|SHAP\| jumped 15× → 1.56). Step 4 (14-fold paired CV): **abandoned** — ablationA regresses 10/14 folds, mean Δ +0.018, worst +0.101 in shocked-regime windows (fold 1 late-2021, fold 9 Israel-Gaza). Diagonal projection drops absolute-anchor info siblings preserve. Followup test queued: `next_session_prompt.md` (non-parametric phase model). | done |
| 2026-06-03 | 2026-06-03_drop_redundant_pair | Drop `station_price_cents` vs `station_minus_last_max_cents` — pair is SHAP-redundant, so removing either should be neutral | Step 1 (5 seeds): drop_price out (sign flips); drop_minus_max real (paired Δ −0.011 ± 0.004). Step 2 (14-fold CV): **abandoned** — 7/14 wins, mean Δ +0.010, two folds regress by >+0.05 (fold 4 +0.066, fold 9 +0.103). Regime-sensitive, not safe to drop. | done |
| 2026-06-03 | redundancy_phase4b_tight | Tighter SHAP redundancy threshold drops fewer real features | — | open |
| 2026-06-03 | redundancy_phase4b_p2 | Second pass on SHAP-based redundancy at relaxed threshold | — | open |
| 2026-06-03 | redundancy_phase4b | Initial SHAP-based feature redundancy sweep | — | open |

<!-- Older dirs (pre-lab-book): brand_leadlag_regime, cv_compare_15feat, cv_compare_phase4, cv_compare_phase4b, l1_per_row_lga, lga_lead_lag, seed_bank_phase4, shap, shap_15feat, shap_phase4, trough_weakness. Backfill rows when revisited. -->
